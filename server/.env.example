# Strapi Server Environment Variables

# Server Configuration
HOST=0.0.0.0
PORT=1337

# Security Keys
# Generate with: openssl rand -base64 32
APP_KEYS="your_app_keys_here,separate_by_comma"
API_TOKEN_SALT=your_api_token_salt
ADMIN_JWT_SECRET=your_admin_jwt_secret
TRANSFER_TOKEN_SALT=your_transfer_token_salt
JWT_SECRET=your_jwt_secret

# Database Configuration (for local development without Docker)
# When using Docker, these are configured in docker-compose.yml
DATABASE_CLIENT=sqlite
DATABASE_FILENAME=.tmp/data.db

# Database Configuration (PostgreSQL - for production or Docker)
# DATABASE_CLIENT=postgres
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_NAME=jewelry_db
# DATABASE_USERNAME=strapi
# DATABASE_PASSWORD=strapi_password
# DATABASE_SSL=false

# Cloudinary Upload Provider (Optional)
# Get credentials from: https://cloudinary.com/console
CLOUDINARY_NAME=your_cloudinary_name
CLOUDINARY_KEY=your_cloudinary_key
CLOUDINARY_SECRET=your_cloudinary_secret
